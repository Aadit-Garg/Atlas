id: atlas.core.logger
name: Console Logger
version: 1.0.0
language: python
roles: [logger, core, stdlib]

execution:
  policy: singleton

communication:
  transports: [memory]
  formats: [python]
  default_format: python

imports: []

exports:
  - capability: atlas.core.logger
    version: 1.0.0

translations: []
